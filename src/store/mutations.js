export default {
        SET_USER_INFO(state, user) {
                state.user = user;
        },
        SET_ITEM_SUMMARY(state, itemSummary) {
                state.itemSummary = itemSummary;
        }

};
